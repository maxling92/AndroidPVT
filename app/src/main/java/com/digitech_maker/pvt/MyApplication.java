package com.digitech_maker.pvt;

import android.app.Application;

public class MyApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();

        // Initialization code or setup that needs to be done once when the application starts

        // Example: Initialize libraries, setup logging, configure global settings, etc.
    }
}
